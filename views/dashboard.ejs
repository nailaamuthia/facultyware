<!doctype html>
<html lang="id">
<head>
  <meta charset="utf-8"/>
  <meta name="viewport" content="width=device-width, initial-scale=1"/>
  <title>Dashboard — Sistem Publikasi B14</title>

  <link rel="preconnect" href="https://fonts.googleapis.com"/>
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin/>
  <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700;800&display=swap" rel="stylesheet"/>
  <link rel="stylesheet" href="/assets/styles.css"/>

  <style>
    body {
      font-family: 'Inter', sans-serif;
      background: #f4f6f8;
    }

    .hero-publikasi {
      background:
        radial-gradient(circle at top right, rgba(255,255,255,0.22), transparent 32%),
        linear-gradient(135deg, #1f2937 0%, #334155 48%, #64748b 100%);
      border-radius: 22px;
      padding: 34px;
      color: white;
      position: relative;
      overflow: hidden;
      margin-bottom: 24px;
      box-shadow: 0 16px 35px rgba(15, 23, 42, 0.18);
    }

    .hero-publikasi::after {
      content: "FTI";
      position: absolute;
      right: 52px;
      top: 34px;
      font-size: 7.2rem;
      font-weight: 800;
      letter-spacing: -8px;
      color: rgba(255,255,255,0.08);
    }

    .hero-badge {
      display: inline-flex;
      align-items: center;
      background: rgba(255,255,255,0.13);
      border: 1px solid rgba(255,255,255,0.20);
      backdrop-filter: blur(8px);
      padding: 8px 14px;
      border-radius: 999px;
      font-size: 0.82rem;
      font-weight: 600;
      margin-bottom: 16px;
    }

    .action-btn {
      background: #475569;
      color: white;
      padding: 10px 16px;
      border-radius: 10px;
      text-decoration: none;
      font-size: 0.84rem;
      font-weight: 700;
      display: inline-flex;
      align-items: center;
      gap: 8px;
    }

    .action-btn:hover {
      background: #334155;
    }

    .action-btn-light {
      background: #ffffff;
      color: #1f2937;
    }

    .action-btn-light:hover {
      background: #e5e7eb;
    }

    .quick-grid {
      display: grid;
      grid-template-columns: repeat(4, 1fr);
      gap: 16px;
      margin-bottom: 24px;
    }

    .quick-card {
      background: #ffffff;
      border: 1px solid #e5e7eb;
      border-radius: 18px;
      padding: 20px 22px;
      box-shadow: 0 8px 22px rgba(15, 23, 42, 0.05);
    }

    .quick-top {
      display: flex;
      align-items: center;
      gap: 14px;
      margin-bottom: 12px;
    }

    .quick-icon {
      width: 48px;
      height: 48px;
      border-radius: 14px;
      display: flex;
      align-items: center;
      justify-content: center;
      flex-shrink: 0;
    }

    .quick-value {
      font-size: 2rem;
      font-weight: 800;
      line-height: 1;
    }

    .quick-label {
      font-size: 0.84rem;
      color: #64748b;
      margin-left: 2px;
    }

    .content-grid {
      display: grid;
      grid-template-columns: 1.5fr 0.9fr;
      gap: 18px;
    }

    .panel {
      background: #ffffff;
      border: 1px solid #e5e7eb;
      border-radius: 18px;
      box-shadow: 0 8px 22px rgba(15, 23, 42, 0.05);
      overflow: hidden;
    }

    .panel-header {
      padding: 20px 22px;
      border-bottom: 1px solid #eef2f7;
      display: flex;
      align-items: center;
      justify-content: space-between;
    }

    .panel-title {
      margin: 0;
      font-size: 1rem;
      font-weight: 700;
      color: #1f2937;
    }

    .panel-desc {
      margin: 4px 0 0;
      font-size: 0.82rem;
      color: #64748b;
    }

    .empty-box {
      text-align: center;
      padding: 52px 24px;
      color: #64748b;
    }

    .empty-circle {
      width: 70px;
      height: 70px;
      border-radius: 50%;
      background: #eef2ff;
      color: #475569;
      display: flex;
      align-items: center;
      justify-content: center;
      margin: 0 auto 16px;
    }

    .mini-list {
      padding: 18px 22px;
      display: grid;
      gap: 12px;
    }

    .mini-item {
      display: flex;
      align-items: flex-start;
      gap: 14px;
      padding: 14px 16px;
      background: #f8fafc;
      border-radius: 14px;
      border: 1px solid #e5e7eb;
      transition: all .2s ease;
    }

    .mini-item:hover {
      background: #eef2f7;
      transform: translateY(-2px);
    }

    .mini-dot {
      width: 12px;
      height: 12px;
      border-radius: 50%;
      background: #4b5563;
      margin-top: 6px;
      flex-shrink: 0;
    }
  </style>
</head>

<body>
  <%- include('partials/sidebar') %>

  <main id="content">
    <%- include('partials/topbar', { pageTitle: 'Dashboard Publikasi' }) %>

    <div class="p-4 md:p-6" style="max-width:1280px; margin:0 auto;">

      <section class="hero-publikasi">
        <div style="position:relative;z-index:1;max-width:760px;">
          <div class="hero-badge">
            Fakultas Teknologi Informasi · Universitas Andalas
          </div>

          <h1 style="font-size:2rem;font-weight:800;margin:0 0 10px;">
            Selamat Datang, <%= user ? user.name : 'Admin' %>
          </h1>

          <p style="font-size:1rem;opacity:0.92;line-height:1.6;margin:0;">
            Sistem ini digunakan untuk mengelola data publikasi dosen FTI, mulai dari pendataan publikasi,
            pengelolaan penulis, hingga kebutuhan export dan API.
          </p>

          <div style="display:flex;gap:12px;margin-top:22px;">
            <a href="/publikasi" class="action-btn action-btn-light">Lihat Publikasi</a>
            <a href="/publikasi/create" class="action-btn">Tambah Publikasi</a>
          </div>
        </div>
      </section>

      <section class="quick-grid">
        <div class="quick-card">
          <div class="quick-top">
            <div class="quick-icon" style="background:#e0e7ff;color:#1e40af;">
              <svg width="22" height="22" fill="none" stroke="currentColor" stroke-width="2" viewBox="0 0 24 24">
                <path d="M4 19.5A2.5 2.5 0 0 1 6.5 17H20"/>
                <path d="M4 4.5A2.5 2.5 0 0 1 6.5 2H20v20H6.5A2.5 2.5 0 0 1 4 19.5z"/>
              </svg>
            </div>
            <div class="quick-value" style="color:#1e40af;"><%= stats ? stats.total : 0 %></div>
          </div>
          <div class="quick-label">Total Publikasi</div>
        </div>

        <div class="quick-card">
          <div class="quick-top">
            <div class="quick-icon" style="background:#fef3c7;color:#b45309;">
              <svg width="22" height="22" fill="none" stroke="currentColor" stroke-width="2" viewBox="0 0 24 24">
                <path d="M12 6v6l4 2"/>
                <circle cx="12" cy="12" r="10"/>
              </svg>
            </div>
            <div class="quick-value" style="color:#b45309;"><%= stats ? stats.proposed : 0 %></div>
          </div>
          <div class="quick-label">Draft / Menunggu</div>
        </div>

        <div class="quick-card">
          <div class="quick-top">
            <div class="quick-icon" style="background:#dbeafe;color:#2563eb;">
              <svg width="22" height="22" fill="none" stroke="currentColor" stroke-width="2" viewBox="0 0 24 24">
                <path d="M9 12l2 2 4-4"/>
                <circle cx="12" cy="12" r="10"/>
              </svg>
            </div>
            <div class="quick-value" style="color:#2563eb;"><%= stats ? stats.completed : 0 %></div>
          </div>
          <div class="quick-label">Terpublikasi</div>
        </div>

        <div class="quick-card">
          <div class="quick-top">
            <div class="quick-icon" style="background:#dcfce7;color:#15803d;">
              <svg width="22" height="22" fill="none" stroke="currentColor" stroke-width="2" viewBox="0 0 24 24">
                <path d="M16 21v-2a4 4 0 0 0-4-4H6a4 4 0 0 0-4 4v2"/>
                <circle cx="9" cy="7" r="4"/>
                <path d="M22 21v-2a4 4 0 0 0-3-3.87"/>
              </svg>
            </div>
            <div class="quick-value" style="color:#15803d;"><%= totalDosen || 0 %></div>
          </div>
          <div class="quick-label">Penulis Terdata</div>
        </div>
      </section>

      <section class="content-grid">
        <div class="panel">
          <div class="panel-header">
            <div>
              <h2 class="panel-title">Publikasi Terbaru</h2>
              <p class="panel-desc">Daftar publikasi yang terakhir dimasukkan ke sistem.</p>
            </div>
            <a href="/publikasi" class="action-btn">Lihat Semua</a>
          </div>

          <div class="empty-box">
            <div class="empty-circle">
              <svg width="30" height="30" fill="none" stroke="currentColor" stroke-width="1.8" viewBox="0 0 24 24">
                <path d="M14.5 2H6a2 2 0 0 0-2 2v16a2 2 0 0 0 2 2h12a2 2 0 0 0 2-2V7.5L14.5 2z"/>
                <polyline points="14 2 14 8 20 8"/>
              </svg>
            </div>
            <h3 style="margin:0 0 6px;color:#334155;">Belum Ada Data Publikasi</h3>
            <p style="margin:0;">Mulai tambahkan data publikasi pertama melalui menu Tambah Publikasi.</p>
          </div>
        </div>

        <div class="panel">
          <div class="panel-header">
            <div>
              <h2 class="panel-title">Fitur Utama Sistem</h2>
              <p class="panel-desc">Modul yang tersedia pada Sistem Publikasi Dosen FTI.</p>
            </div>
          </div>

          <div class="mini-list">
            <div class="mini-item">
              <span class="mini-dot"></span>
              <div><b>Kelola Publikasi</b><br><small>Melihat dan mengelola seluruh data publikasi dosen.</small></div>
            </div>

            <div class="mini-item">
              <span class="mini-dot"></span>
              <div><b>Tambah Data Publikasi</b><br><small>Menambahkan data publikasi baru ke dalam sistem.</small></div>
            </div>

            <div class="mini-item">
              <span class="mini-dot"></span>
              <div><b>Kelola Penulis</b><br><small>Mengatur data penulis yang terlibat dalam publikasi.</small></div>
            </div>

            <div class="mini-item">
              <span class="mini-dot"></span>
              <div><b>Approval Publikasi</b><br><small>Memantau status persetujuan dan validasi publikasi.</small></div>
            </div>

            <div class="mini-item">
              <span class="mini-dot"></span>
              <div><b>Export & API</b><br><small>Menyediakan export data dan layanan API publikasi.</small></div>
            </div>
          </div>
        </div>
      </section>

    </div>
  </main>

  <script src="/assets/js/basecoat.js" defer></script>
  <script src="/assets/js/sidebar.js" defer></script>
  <script src="/assets/js/popover.js" defer></script>
  <script src="/assets/js/toast.js" defer></script>
</body>
</html>